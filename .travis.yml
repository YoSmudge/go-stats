language: go

go:
  - 1.5
  - 1.6
  - tip

install:
  - go get github.com/Masterminds/glide
  - ${GOPATH}/bin/glide install

allow_failures:
  - go: tip

script:
  - make test

env:
  - "GO15VENDOREXPERIMENT=1"
